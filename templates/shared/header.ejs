<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <% if (page == 'index') { %> 
        <% if (locals.user) { %>
            <form action="/logout" method="post" class="text-end mt-2 me-2">
                <button class="btn btn-danger">
                    Se déconnecter
                </button>
            </form>
        <% } else { %>
            <form action="/login" method="get" class="text-end mt-2 me-2">
                <button class="btn btn-primary">
                    Se connecter
                </button>
            </form>
        <% } %>
    <% } %>